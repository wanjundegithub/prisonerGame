<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.prisoner.game.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.company.prisoner.game.model.User">
        <id property="id" column="id" />
        <result property="nickName" column="nick_name" />
        <result property="userName" column="user_name" />
        <result property="password" column="password" />
        <result property="role" column="role"/>
        <result property="className" column="class_name"/>
        <result property="createBy" column="create_by" />
        <result property="createDate" column="create_date" />
        <result property="lastUpdateBy" column="last_update_by" />
        <result property="lastUpdateDate" column="last_update_date" />
        <result property="deleteFlag" column="delete_flag" />
    </resultMap>


    <sql id="userCondition">
        <if test="id!=null">
            AND  id = #{id}
        </if>
        <if test="userName!=null and userName!=''">
            AND  user_name = #{userName}
        </if>
        <if test="password!=null and password!=''">
            AND password = #{password}
        </if>
        <if test="nickName!=null and nickName!=''">
            AND nick_name = #{nickName}
        </if>
        <if test="role!=null and role!=''">
            AND `role` = #{role}
        </if>
        <if test="className!=null and className!=''">
            AND `class_name` = #{className}
        </if>
        <if test="userNameList!=null">
            AND  user_name in
            <foreach item="userName" index="index" collection="userNameList" open="(" separator="," close=")">
                #{userName}
            </foreach>
        </if>
    </sql>

    <select id="getAllUsers" resultMap="UserResultMap">
        SELECT
            *
        FROM
            user
    </select>

    <select id="selectTotal" resultType="integer">
        select count(*) from user where 1=1
        <include refid="userCondition"/>
    </select>

    <select id="selectUserList" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE 1=1
        <include refid="userCondition"/>
        <if test="page!=null and pageSize!=null">
            limit #{offset}, #{pageSize}
        </if>
    </select>

    <select id="selectUserById" resultMap="UserResultMap">
        SELECT * FROM `user`
        WHERE id = #{id}
    </select>

    <update id="updateUser">
        UPDATE user
        SET
        <if test="userName != null and userName != ''">
            user_name = #{userName},
        </if>
        <if test="nickName != null and nickName != ''">
            nick_name = #{nickName},
        </if>
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="className != null and className != ''">
            class_name = #{className},
        </if>
        <if test="lastUpdateBy != null and lastUpdateBy != ''">
            last_update_by = #{lastUpdateBy},
        </if>
            last_update_date = NOW()
        WHERE id = #{id}
    </update>

    <update id="batchUpdateUserList" parameterType="java.util.List">
        <foreach item="user" index="index" collection="userList" separator=";">
            UPDATE `user`
            SET
            <if test="user.userName != null and user.userName != ''">
                `user_name` = #{user.userName},
            </if>
            <if test="user.nickName != null and user.nickName != ''">
                `nick_name` = #{user.nickName},
            </if>
            <if test="user.password != null and user.password != ''">
                `password` = #{user.password},
            </if>
            <if test="user.className != null and user.className != ''">
                `class_name` = #{user.className},
            </if>
            <if test="user.role != null and user.role != ''">
                `role` = #{user.role},
            </if>
            <if test="user.lastUpdateBy != null and user.lastUpdateBy != ''">
                `last_update_by` = #{user.lastUpdateBy},
            </if>
            `last_update_date` = NOW()
            WHERE `id` = #{user.id}
        </foreach>
    </update>

    <!-- 单个插入 -->
    <insert id="insertUser" parameterType="com.company.prisoner.game.model.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (nick_name, user_name, password, role, class_name,  create_by, create_date, last_update_by, last_update_date, delete_flag)
        VALUES (#{nickName}, #{userName}, #{password}, #{role}, #{className}, #{createBy}, now(), #{lastUpdateBy}, now(), 0)
    </insert>

    <!-- 删除 -->
    <delete id="deleteUserById" parameterType="Integer">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

    <delete id="deleteUserList" parameterType="java.util.List">
        DELETE FROM `user`
        WHERE id IN
        <foreach item="item" index="index" collection="idList" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 批量插入 -->
    <insert id="insertUsers" parameterType="java.util.List"  useGeneratedKeys="true" keyProperty ="id">
        INSERT INTO `user` (nick_name, user_name, password, role, class_name, create_by, create_date, last_update_by, last_update_date, delete_flag)
        VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.nickName}, #{user.userName}, #{user.password}, #{user.role}, #{user.className}, #{user.createBy}, now(), #{user.lastUpdateBy}, now(), 0)
        </foreach>
    </insert>

</mapper>
